{% extends 'base.html.twig' %}

{% block body %}

{% if error %}
    <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
{% endif %}


<div class = "container">
	<div class="wrapper">
		<form action="{{ path('login') }}" method="post"  class="form-signin">
		    <h3 class="form-signin-heading">Bienvenido de nuevo. Logueate para entrar</h3>
			  <hr class="colorgraph"><br>

			  <input type="text" value="{{ last_username }}" class="form-control" id="username" name="_username" placeholder="Usuario" required="" autofocus="" />
			  <input type="password" class="form-control" id="password" name="_password" placeholder="Contraseña" required=""/>

			  <button class="btn btn-lg btn-primary btn-block"  name="submit" value="Login" type="Submit">Login</button>

    </form>

    </div>
    </div>

{% endblock %}
